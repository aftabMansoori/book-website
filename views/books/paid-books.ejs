<%- include('../layouts/header') %> 
<div class="p-4">
    <h2 class="text-center book-header">Paid Books</h2><hr class="hr-underline">
</div>

<div class="add mt-4">       
    <div class="row">
    <% book.forEach(book => { %>
        <div class="col-md-3 col-6 mt-3">
          <div class="book">
            <center>
              <img class="img-fluid" src="<%= book.coverImagePath %>" />
            </center>
          </div>
          <div class="mt-1">
            <p class="h3 book-title"><%= book.name %></p>
            <p class="h3 book-title"><%= book.title %></p>
            <p class="small text-muted"><%= book.createdAt.toLocaleDateString() %></p>
            <p><span class="book-head">Genre: </span><%= book.genre %></p>
            <p><span class="book-head">Price: </span>Rs.<%= book.price %></p>
            <form action="/books/purchased/<%= book.id %>" method="POST">
                <button type="submit" class="btn btn-success m-2">Buy Book</button>
            </form>
          </div>
        </div>
        <% }) %> 
    </div>
</div>

<%- include('../layouts/footer') %> 